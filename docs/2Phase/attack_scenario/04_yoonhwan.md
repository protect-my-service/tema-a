# 공격 시나리오 템플릿

#### [기능 A] 주문 생성 - TODO

| ID      | 시나리오명          | 키워드               | 목표 가설                                                           | 가설 근거(코드)                                                                | 트래픽/패턴                             | 예상 문제점                                 | 기술적 근거                                                                            | 설계자   | 엔드포인트                 |
| ------- | -------------- | ----------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------- | -------------------------------------- | --------------------------------------------------------------------------------- | ----- | --------------------- |
| ATK-A01 | DB 리소스 소진      | `리소스소진` `락경합`     | 동일 상품에 비관적 락 다수 요청 → HikariCP(max=10) 고갈 → DB/Application 모두 다운 | `OrderService.createOrder()`  <br>`findByIdWithLock` (PESSIMISTIC_WRITE) | 100→500→1,000 VU  <br>Stepped, 10분 | DB Connection 부하 / Pessimistic Lock 대기 | `SELECT FOR UPDATE` 경합, `lock.timeout=3000`, `SqlTransientConnectionException` 발생 | 김준성   | `POST /api/v1/orders` |
| ATK-A02 | 한정판 단일 상품 락 경합 | `Spike` `Hot row` | 한정판 상품 1개에 동시 주문 → 락 대기 큐 폭증 → timeout 후 줄줄이 실패                 | `findByIdWithLock` + `lock.timeout: 3000ms`                              | 1,000 VU 동시  <br>Spike, 5분         | 락 대기 시간 폭증 / 일부만 성공                    | 비관적 락 시리얼라이즈 → TPS = 1000ms / 트랜잭션 길이                                             | (TBD) | `POST /api/v1/orders` |
| ATK-A03 | 카트 N+1 락 누적    | `다중 락`            | 카트에 다수 상품 담고 동시 주문 → 트랜잭션당 락 N개 누적 → 점유 시간 증가                   | `OrderService.createOrder()` for-loop 내 `findByIdWithLock`               | 200 VU  <br>Constant, 10분          | 트랜잭션 점유 시간 증가 / 데드락 가능성                | 락 N개를 한 트랜잭션에서 잡고 추가 작업(`entityManager.refresh`, deleteAll) 수행                    | (TBD) | `POST /api/v1/orders` |

---

#### [기능 B] 결제 요청 - TODO

| ID      | 시나리오명                | 키워드                     | 목표 가설                                                    | 가설 근거(코드)                                                                                               | 트래픽/패턴                       | 예상 문제점                         | 기술적 근거                                                   | 엔드포인트                   | 설계자   |
| ------- | -------------------- | ----------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------ | -------------------------------------------------------- | ----------------------- | ----- |
| ATK-B01 | 외부 PG 트랜잭션 점유        | `외부의존성` `Connection 점유` | 외부 PG 호출이 트랜잭션 내부 → 평균 472ms 동안 커넥션 점유 → 25 TPS 넘으면 풀 고갈 | `PaymentService.requestPayment()`  <br>`@Transactional` 안에서 `externalPaymentClient.requestPayment()` 호출 | 30→100 VU  <br>Stepped, 10분  | 커넥션 풀 고갈 / 신규 요청 30초 대기 후 타임아웃 | HikariCP max=10, PG 평균 400ms (2% 타임아웃 5s) → 이론 한계 21 TPS | `POST /api/v1/payments` | (TBD) |
| ATK-B02 | 중복 결제 Race Condition | `동시성` `락 누락`            | 같은 orderId에 동시 결제 요청 2건 → 중복 체크에 락 없어 둘 다 통과             | `PaymentService.requestPayment()`  <br>`findByOrderIdAndStatusNot` (락 없음)                               | 동일 orderId 동시 2~5 요청 × 100세트 | 이중 결제 / 데이터 정합성 깨짐             | 중복 체크와 INSERT 사이 시간차 + 락 없음 → race                       | `POST /api/v1/payments` | (TBD) |
| ATK-B03 | PG 타임아웃 누적           | `Slow Path`             | 2% 확률 5초 타임아웃이 누적되면 커넥션 풀 빠르게 소진                         | `ExternalPaymentClient.requestPayment()` 2% 확률 3~5초 sleep                                               | 50 VU 지속  <br>Constant, 15분  | 평균 응답시간 우상향 / 풀 고갈             | 타임아웃 동안 트랜잭션이 커넥션 점유                                     | `POST /api/v1/payments` | (TBD) |

---

#### [기능 C] 주문 취소 - TODO

| ID      | 시나리오명       | 키워드         | 목표 가설                                                   | 가설 근거(코드)                                                                  | 트래픽/패턴                              | 예상 문제점                  | 기술적 근거                                                  | 엔드포인트                             | 설계자   |
| ------- | ----------- | ----------- | ------------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------- | ----------------------- | ------------------------------------------------------- | --------------------------------- | ----- |
| ATK-C01 | 의도된 데드락 유도  | `데드락` `락순서` | 두 주문이 상품 락 순서를 역순으로 잡으면 InnoDB 데드락 발생                   | `OrderService.cancelOrder()` 주석: "의도된 공격 표면 — 데드락 유도 가능"  <br>입력 순서대로 락 잡음 | 100쌍 동시(주문A, 주문B 역순)  <br>Spike, 1분 | 데드락 / HTTP 500 / 사용자 노출 | `Deadlock found when trying to get lock`  <br>재시도 로직 없음 | `POST /api/v1/orders/{id}/cancel` | (TBD) |
| ATK-C02 | 취소 윈도우 내 폭주 | `대량취소`      | PAID 후 1시간 윈도우 내 대량 취소 → Order락 + N개 Product락 누적 → 풀 고갈 | `OrderService.cancelOrder()` Order 비관적 락 + for-loop Product 락              | 500 VU  <br>Stepped, 10분            | DB 락 점유 시간 누적 / 풀 고갈    | `findByIdAndMemberIdForUpdate` + N개 `findByIdWithLock`  | `POST /api/v1/orders/{id}/cancel` | (TBD) |

---

#### [기능 D] 결제 취소 - TODO

| ID      | 시나리오명           | 키워드            | 목표 가설                                             | 가설 근거(코드)                                                                              | 트래픽/패턴                   | 예상 문제점                 | 기술적 근거                           | 엔드포인트                                | 설계자   |
| ------- | --------------- | -------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------ | ---------------------- | -------------------------------- | ------------------------------------ | ----- |
| ATK-D01 | 외부 PG 취소 + 다중 락 | `외부의존성` `다중 락` | PG 취소 호출(200~400ms) 동안 Order락 + N개 Product락 누적 점유 | `PaymentService.cancelPayment()`  <br>`@Transactional` 내부 외부호출 + N개 `findByIdWithLock` | 50 VU  <br>Constant, 10분 | 커넥션+락 동시 점유 → 풀 빠르게 고갈 | 외부 PG 호출이 트랜잭션 내부 + 후행 Product 락 | `POST /api/v1/payments/{key}/cancel` | (TBD) |

---

#### [기능 E] 기타 - TODO

| ID      | 시나리오명      | 키워드           | 목표 가설                                                       | 가설 근거(코드)                                                            | 트래픽/패턴                            | 예상 문제점         | 기술적 근거                           | 엔드포인트            | 설계자 |
| ------- | ---------- | ------------- | ----------------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------- | -------------- | -------------------------------- | ---------------- | --- |
| ATK-E01 | 어플리케이션 OOM | `서버부하` `헤더공격` | Header `X-Member-Id` 검증 없이 사용 → 10MB 긴 텍스트로 공격 시 메모리/CPU 부하 | `CartController` `@RequestHeader("X-Member-Id") Long memberId` (미검증) | 50 VU + 10MB 헤더  <br>Constant, 5분 | OOM / GC pause | 헤더 입력 검증 부재, Spring Security 미적용 | GET /api/v1/cart |     |
