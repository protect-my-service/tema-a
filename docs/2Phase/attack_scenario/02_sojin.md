# 공격 시나리오

> **작성 기준일**: 2026-04-27 (미팅: 2026-04-28)
> 
> **대상 프로젝트**: pms-order / pms-order-bteam / pms-coupon / pms-queue
> 
> **ID 체계**: `ATK-{프로젝트}-{기능}{순번}` — `O`=order, `C`=coupon, `Q`=queue. (pms-order-bteam은 pms-order와 코드가 동일하므로 동일 시나리오 적용 가능 → 별도 표기 생략, 필요 시 `ATK-OB-*`로 미러링.)

## 1. pms-order (= pms-order-bteam)

### [기능 A] 주문 생성

| ID         | 시나리오명          | 키워드               | 목표 가설                                                            | 가설 근거(코드)                                                                                                            | 트래픽/패턴                              | 예상 문제점                          | 기술적 근거                                                                              | 설계자 | 엔드포인트                 |
| ---------- | -------------- | ----------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------- | ---------------------------------------------------------------------------------- | --- | --------------------- |
| ATK-O-A01  | DB 리소스 소진      | `리소스소진` `락경합`     | 동일 상품에 비관적 락 다수 요청 → HikariCP(max=10) 고갈 → DB/Application 모두 다운  | `OrderService.createOrder()` 내부 for-loop이 각 `CartItem`마다 `ProductRepository.findByIdWithLock()` 호출 (`@Lock(PESSIMISTIC_WRITE)`) | 100 → 500 → 1,000 VU<br>Stepped, 10분 | DB Connection 부하 / Pessimistic Lock 대기 큐 폭증 | `SELECT ... FOR UPDATE` 직렬화, `lock.timeout=3000ms` 초과 시 `LockTimeoutException` + `SqlTransientConnectionException` 동반 발생 | 김준성 | `POST /api/v1/orders` |
| ATK-O-A02  | 한정판 단일 상품 락 경합 | `Spike` `Hot row` | 한정판 상품 1개에 동시 주문 → 락 대기 큐 폭증 → 3s timeout 후 줄줄이 실패              | `ProductRepository.findByIdWithLock()` + `application.yml`의 `jakarta.persistence.lock.timeout: 3000`                  | 1,000 VU 동시<br>Spike, 5분            | 락 대기 시간 폭증 / 일부만 성공             | 비관적 락 시리얼라이즈 → 이론 TPS ≈ 1000ms / 트랜잭션 길이. 트랜잭션 수백 ms 가정 시 단일 row TPS는 한 자릿수    | (TBD) | `POST /api/v1/orders` |
| ATK-O-A03  | 카트 N+1 락 누적    | `다중 락` `데드락`     | 카트에 다수 상품 담고 동시 주문 → 트랜잭션당 락 N개 누적 → 점유 시간 증가 + 입력 순서 뒤집으면 데드락 | `OrderService.createOrder()` for-loop 내 `findByIdWithLock` + `entityManager.refresh(product)` 호출 + 루프 종료 후 `cartItemRepository.deleteAll(cartItems)` | 200 VU<br>Constant, 10분             | 트랜잭션 점유 시간 증가 / 데드락 가능성        | 한 트랜잭션에서 락 N개 점유한 채 `refresh`/`deleteAll` 후행 작업. 동시 요청 간 `cartItems` 입력 순서 차이 → 락 순서 충돌 | (TBD) | `POST /api/v1/orders` |

### [기능 B] 결제 요청

| ID         | 시나리오명                | 키워드                     | 목표 가설                                                            | 가설 근거(코드)                                                                                                                                 | 트래픽/패턴                          | 예상 문제점                          | 기술적 근거                                                                                                       | 엔드포인트                   | 설계자 |
| ---------- | -------------------- | ------------------------ | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------- | --- |
| ATK-O-B01  | 외부 PG 트랜잭션 점유        | `외부의존성` `Connection 점유`  | 외부 PG 호출이 트랜잭션 내부 → 평균 약 472ms 동안 커넥션 점유 → 약 21 TPS 넘으면 풀 고갈    | `PaymentService.requestPayment()`가 `@Transactional` 메서드 내부에서 `ExternalPaymentClient.requestPayment(...)` 호출                            | 30 → 100 VU<br>Stepped, 10분    | 커넥션 풀 고갈 / 신규 요청 30s `connection-timeout` 후 타임아웃 | HikariCP max=10, PG 평균 ≈ 0.98×400ms + 0.02×4000ms ≈ 472ms → 이론 한계 ≈ 10/0.472 ≈ 21 TPS | `POST /api/v1/payments` | (TBD) |
| ATK-O-B02  | 중복 결제 Race Condition | `동시성` `락 누락`             | 동일 orderId로 동시 결제 요청 2건 → 중복 체크에 락 없어 둘 다 통과 → 외부 PG 이중 호출       | `PaymentService.requestPayment()` 의 중복 체크가 `paymentRepository.findByOrderIdAndStatusNot(orderId, FAILED)` (락 없는 일반 SELECT)              | 동일 orderId 동시 2~5 요청 × 100세트 (Spike) | 이중 결제 / 데이터 정합성 깨짐             | 락 없는 중복 체크 → INSERT 사이 race window. 두 트랜잭션 모두 통과 후 외부 호출까지 진행                          | `POST /api/v1/payments` | (TBD) |
| ATK-O-B03  | PG 타임아웃 누적           | `Slow Path`              | 2% 확률 3~5초 sleep이 누적되면 트랜잭션이 커넥션을 길게 점유 → 풀 빠르게 소진             | `ExternalPaymentClient.requestPayment()` 의 2% 확률 분기에서 `Thread.sleep(3000~5000)` 호출                                                       | 50 VU 지속<br>Constant, 15분      | 평균 응답시간 우상향 / 풀 고갈           | 타임아웃 동안 트랜잭션이 커넥션 점유. 풀 10개 기준 동시 4~5건 slow path만으로 신규 요청 차단 가능                                | `POST /api/v1/payments` | (TBD) |

### [기능 C] 주문 취소

| ID         | 시나리오명       | 키워드          | 목표 가설                                                          | 가설 근거(코드)                                                                                                       | 트래픽/패턴                              | 예상 문제점                  | 기술적 근거                                                                                                  | 엔드포인트                             | 설계자 |
| ---------- | ----------- | ------------ | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------- | --------------------------------- | --- |
| ATK-O-C01  | 의도된 데드락 유도  | `데드락` `락순서`  | 두 주문이 상품 락 순서를 역순으로 잡으면 PG 데드락 발생 → victim 트랜잭션 강제 abort      | `OrderService.cancelOrder()` for-loop이 `targets` 리스트(요청 입력 순서) 그대로 `findByIdWithLock` 호출, `product.id` 기준 정렬 방어 없음 | 100쌍 동시 (주문A: [X,Y], 주문B: [Y,X])<br>Spike, 1분 | 데드락 / HTTP 500 / 사용자 노출 | PG `deadlock detected`, 재시도 로직 없음. `targets`를 `product.id` 오름차순 정렬 후 락 획득하는 방어가 미적용                                          | `POST /api/v1/orders/{id}/cancel` | (TBD) |
| ATK-O-C02  | 취소 윈도우 내 폭주 | `대량취소` `다중락` | PAID 후 1시간 윈도우 내 대량 취소 → Order 락 + N개 Product 락 누적 → 풀 고갈      | `OrderService.cancelOrder()` 시작부의 `orderRepository.findByIdAndMemberIdForUpdate()` (Order 비관적 락) + 이어지는 for-loop의 `findByIdWithLock` (Product 락) | 500 VU<br>Stepped, 10분            | DB 락 점유 시간 누적 / 풀 고갈 | Order 락 + N개 `findByIdWithLock`을 한 트랜잭션에서 점유 → 동시 취소 다발 시 풀/락 모두 압박                            | `POST /api/v1/orders/{id}/cancel` | (TBD) |

### [기능 D] 결제 취소

| ID         | 시나리오명           | 키워드             | 목표 가설                                                  | 가설 근거(코드)                                                                                                                  | 트래픽/패턴                  | 예상 문제점                 | 기술적 근거                                            | 엔드포인트                                | 설계자 |
| ---------- | --------------- | --------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ---------------------- | ------------------------------------------------- | ------------------------------------ | --- |
| ATK-O-D01  | 외부 PG 취소 + 다중 락 | `외부의존성` `다중 락` | PG 취소 호출(수백 ms) 동안 Order 락 + N개 Product 락 누적 점유      | `PaymentService.cancelPayment()` `@Transactional` 메서드 내부에서 `ExternalPaymentClient.cancelPayment(paymentKey)` 호출 → 직후 `order.getItems()` for-loop에서 `findByIdWithLock` | 50 VU<br>Constant, 10분 | 커넥션 + 락 동시 점유 → 풀 빠르게 고갈 | 외부 PG 호출이 트랜잭션 내부 + 후행 Product 락 → 커넥션·락 두 자원이 동시에 묶임 | `POST /api/v1/payments/{key}/cancel` | (TBD) |

### [기능 E] 기타 (인증/검증)

| ID         | 시나리오명      | 키워드            | 목표 가설                                                       | 가설 근거(코드)                                                                                                | 트래픽/패턴                            | 예상 문제점         | 기술적 근거                          | 엔드포인트            | 설계자 |
| ---------- | ---------- | -------------- | ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------- | -------------- | ------------------------------- | ---------------- | --- |
| ATK-O-E01  | 어플리케이션 OOM | `서버부하` `헤더공격` | Header `X-Member-Id` 검증 없이 사용 → 10MB 긴 텍스트로 공격 시 메모리/CPU 부하 | `CartController` 의 4개 핸들러(`getCart` / `addItem` / `updateItemQuantity` / `removeItem`) 모두 `@RequestHeader("X-Member-Id") Long memberId` 만 받고 길이/형식 검증 없음 | 50 VU + 10MB 헤더<br>Constant, 5분 | OOM / GC pause | 헤더 입력 검증 부재, Spring Security 미적용 | `GET /api/v1/cart` | (TBD) |

---

## 2. pms-coupon

### [기능 A] 쿠폰 발급 (Redis 1차 + DB 2차)

| ID         | 시나리오명             | 키워드               | 목표 가설                                                                                                       | 가설 근거(코드)                                                                                                                                 | 트래픽/패턴                              | 예상 문제점                         | 기술적 근거                                                                                                   | 설계자 | 엔드포인트                       |
| ---------- | ----------------- | ----------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------ | --- | --------------------------- |
| ATK-C-A01  | 비관적 락 큐 폭주 (DoS)  | `리소스소진` `락경합`    | 단일 쿠폰에 동시 발급 요청 다발 → DB 비관적 락 시리얼라이즈 → DB 풀 고갈 + lock.timeout 미설정으로 무한 대기                                  | `CouponIssueTxService.issueWithLock()` 가 `CouponCustomImpl.findByIdWithPessimisticLock()` 호출 (QueryDSL `setLockMode(PESSIMISTIC_WRITE)`)            | 1,000 → 5,000 → 10,000 VU<br>Stepped, 10분 | 정상 사용자 응답 시간 N배 증가 / 503     | 단일 row 락 시리얼라이즈. `application.yml`에 `lock.timeout` 미설정 → PG 기본값(무한) 대기. 풀 기본 10개 → 즉시 고갈   | (TBD) | `POST /coupons/{id}/issue`  |
| ATK-C-A02  | PROCESSING 좀비    | `분산락` `TTL잠김`    | 발급 요청 직후 TCP RST → ACQUIRED 상태인 PROCESSING 키만 잔존 → 동일 회원 재요청은 쿠폰 종료까지 IN_PROGRESS 영구 차단                    | `CouponIssueRedisService.acquireMemberRequest()` 의 `setIfAbsent(memberRequestKey, "PROCESSING", ttl)` + TTL을 `CouponIssueService.calculateCouponTtl()` 가 쿠폰 종료시각+1s로 산출 | 100 회원 × `curl --max-time 0.05` 강제 RST | 정상 회원의 발급 영구 차단              | TTL이 쿠폰 종료까지로 길어 좀비가 자연 소멸 안 함. RST 경로에서는 catch의 `deleteMemberRequest`가 실행되지 않음                  | (TBD) | `POST /coupons/{id}/issue`  |
| ATK-C-A03  | 같은 사용자 동시 다중      | `중복요청` `UX저하`    | 동일 X-Member-Id로 동시 100건 → 1건만 ACQUIRED, 99건 IN_PROGRESS → 사용자 새로고침해도 동일 응답                                 | `CouponIssueRedisService.acquireMemberRequest()` 의 `setIfAbsent` 멱등 체크                                                                              | 동일 memberId × 100 동시 요청 (Spike)    | 사용자 UX 저하 (CS 인입)             | 단일 회원 동시 다중 요청은 비즈니스적으로 정상 응답이지만, 사용자는 “왜 발급 안 되지” 혼란 → 환불/사과 트래픽으로 직결                | (TBD) | `POST /coupons/{id}/issue`  |

### [기능 B] Redis-DB 정합성 / 이벤트 발행

| ID         | 시나리오명                       | 키워드                  | 목표 가설                                                                                                                              | 가설 근거(코드)                                                                                                                            | 트래픽/패턴                                | 예상 문제점                          | 기술적 근거                                                                                                            | 설계자 | 엔드포인트                       |
| ---------- | --------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------- | --- | --------------------------- |
| ATK-C-B01  | DECR 비대칭 → 가짜 매진            | `정합성` `보상실패`        | Redis 일시 단절 시점에 발급 폭격 → DB INSERT 성공한 트랜잭션이 후행 `increaseIssuedCount`/`markMemberRequestDone` 실패. catch는 `issuePersisted=true`라 DECR도 안 함 → requested-count 영구 +1 | `CouponIssueService.issue()` 의 `RuntimeException` catch 블록 진실표 (`requestedCountIncreased && !issuePersisted` 일 때만 `decreaseRequestedCount` 호출), `CouponIssueRedisService.decreaseRequestedCount()` 가 보상 분기에서만 호출됨 | toxiproxy로 Redis 1~3s pause × 100 VU 발급 | requested-count > 발급 수 → 조기 SOLD_OUT (가짜 매진) | DB 커밋 후 Redis 후속 작업 비대칭. catch가 `issuePersisted` 플래그를 보고 DECR 분기 결정 → 단절 윈도우 들어간 요청은 DECR 누락        | (TBD) | `POST /coupons/{id}/issue`  |
| ATK-C-B02  | AFTER_COMMIT Redis 초기화 누락 | `이벤트유실` `fallback의존` | 쿠폰 생성 직후 Redis 다운 → AFTER_COMMIT 리스너의 `initializeCouponKeys` 실패, 예외 로그만 → fallback(`hasCouponKeys → init`)에 race window 의존 | `CouponRedisSyncListener.onCouponCreated()` 가 `@TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)` + `try { ... } catch (Exception e) { log.error(...) }` 로 예외 삼킴 | 쿠폰 생성 + 즉시 Redis 단절 + 동시 발급 100 VU | 정상 발급 흐름이 fallback에 의존, race window 발생 | Outbox 부재 + 예외 삼킴. 리스너 실패 시 운영자 조기 인지 불가                                                          | (TBD) | `POST /coupons` → `/issue` |
| ATK-C-B03  | DataIntegrityViolation 위장 | `에러은닉` `정찰`        | FK/NOT NULL 등 모든 DataIntegrity 예외를 ALREADY_ISSUED로 위장 → 운영자가 진짜 정합성 사고를 못 알아챔                                     | `CouponIssueTxService.issueWithLock()` 의 `catch (DataIntegrityViolationException e)` → `throw new BusinessCustomException(ResponseCode.COUPON_ALREADY_ISSUED)` (예외 종류 구분 없이 동일 코드로 변환) | 정찰성 잘못된 페이로드 + 정상 트래픽 혼합            | 진짜 사고가 멱등 응답에 묻혀 SLA 위반        | UNIQUE 위반과 그 외 무결성 위반(FK/NOT NULL)이 동일 응답 코드로 변환됨                                              | (TBD) | `POST /coupons/{id}/issue`  |

---

## 3. pms-queue

### [기능 A] 이벤트 적재 (Producer + 백프레셔)

| ID         | 시나리오명                  | 키워드                  | 목표 가설                                                                                                                | 가설 근거(코드)                                                                                          | 트래픽/패턴                                       | 예상 문제점                            | 기술적 근거                                                                                                          | 설계자 | 엔드포인트          |
| ---------- | ---------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------- | --------------------------------- | --------------------------------------------------------------------------------------------------------- | --- | -------------- |
| ATK-Q-A01  | Stream MAXLEN 부재 → OOM | `메모리폭파` `백프레셔우회`    | XADD에 MAXLEN 미설정 → Rate Limit 통과분만으로도 누적 → Redis 메모리 폭파                                                              | `RedisEventProducer.publish()` 가 `redisTemplate.opsForStream().add(record)` 만 호출 (MAXLEN 옵션 미적용) | 100 스레드 × payload 100KB, 12시간 지속            | Redis OOM / 시스템 다운             | XADD 무한 적재 + 초당 10건 × 86400초 = 86만 건/일. 100KB payload → ~86GB/일                                | (TBD) | `POST /events` |
| ATK-Q-A02  | Rate Limiter Fixed Window 트릭 | `Bypass` `Spike`     | Resilience4j RateLimiter가 Fixed Window라 매 초 카운터 리셋 → 초 경계에서 한도 2배 통과                                                  | `application.properties` 의 `resilience4j.ratelimiter.instances.events.limit-for-period=10`, `limit-refresh-period=1s` (Sliding Window 미설정 → Fixed Window) | NTP 동기화 100 클라이언트, 매 초 0.995~1.005s 폭격    | 한도 2배 트래픽 통과 → 다운스트림 부하       | 0.999s에 10건 + 1.001s에 10건 가능. Sliding Window면 방어 가능                                                 | (TBD) | `POST /events` |
| ATK-Q-A03  | Circuit Breaker 토글 유도   | `장애전파` `503증폭`      | 의도적 실패 트래픽 추가 → 합산 실패율 50% 초과 → OPEN → 정상 트래픽도 503 → HALF-OPEN 3건 테스트에 공격 트래픽 끼어 다시 OPEN 무한 반복                | `application.properties` 의 `resilience4j.circuitbreaker.instances.events.failure-rate-threshold=50`, `wait-duration-in-open-state=10s`, `permitted-number-of-calls-in-half-open-state=3`, `sliding-window-size=10` | 정상 100 RPS + 공격 100 RPS (BadRequest 유도)    | 정상 사용자도 10초 단위로 503 받음          | sliding-window=10에 실패율 50% → 5건 실패로 OPEN. HALF-OPEN 3건 중 1건만 공격이면 다시 OPEN                | (TBD) | `POST /events` |

### [기능 B] 이벤트 조회/소비 (Consumer)

| ID         | 시나리오명                          | 키워드                   | 목표 가설                                                                                       | 가설 근거(코드)                                                                                                | 트래픽/패턴                       | 예상 문제점                         | 기술적 근거                                                                                                   | 설계자 | 엔드포인트                |
| ---------- | ------------------------------ | --------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------- | --- | -------------------- |
| ATK-Q-B01  | Range.unbounded() 전체 조회 OOM     | `메모리폭파` `대량조회`       | ATK-Q-A01과 결합: Stream에 100만 건 쌓은 후 `GET /events` 단일 호출 → 전체 조회 → JVM heap 폭파              | `EventController.getEvents()` 가 `redisTemplate.opsForStream().range(STREAM_KEY, Range.unbounded())` 호출 (페이징 없음)                     | A01 폭격 후 단일 GET 요청          | OOM / GC pause / 서버 다운        | 모든 메시지를 `Map`으로 변환 후 응답 → JVM heap 한 번에 점유                                       | (TBD) | `GET /events`        |
| ATK-Q-B02  | Consumer 단일 + 멱등성 부재 → 중복/정체 | `At-least-once` `수평확장X` | `consumer-1` 하드코딩 + 비즈니스 로직 후 XACK → 처리 직전 크래시 시 재처리 → 멱등성 없으면 중복 부수효과. 동일 이름 재배포 시 분배 실패  | `RedisEventConsumer.CONSUMER_NAME = "consumer-1"` 상수, `start()`에서 `Consumer.from(GROUP_NAME, CONSUMER_NAME)`로 단일 등록, `onMessage()`가 비즈니스 로직(현재는 `System.out.println`) 후 마지막 줄에서 `redisTemplate.opsForStream().acknowledge(...)` 호출 | 1,000 RPS 적재 + Consumer kill -9 반복 | 메시지 정체 / 재시작 시 중복 처리         | 단일 Consumer = 단일 스레드 처리량. 하드코딩 이름 → 수평 확장 시 동일 이름 등록되어 메시지 분배 안 됨                | (TBD) | (Consumer 내부)        |

