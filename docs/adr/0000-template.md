# NNNN. {결정 제목}

- 상태: 제안 | 채택 | 폐기 | 대체됨(→ NNNN)
- 날짜: YYYY-MM-DD
- 작성자:
- 관련: W3 비동기 순서 보장 / 선택 옵션(A Outbox / B Saga / C Kafka 파티션키 / D Confirms+DLQ / E CDC)

## Context (맥락)

어떤 문제/제약 때문에 이 결정이 필요했는가. 측정으로 드러난 사실을 인용한다.
예: "ATK-C03 재현 시 RabbitMQ stop 동안 유실률 __%, 중복 __건, 순서 꼬임 __% 관측."

## Decision (결정)

무엇을 택했는가. 한 문장으로 명확히.

## 고려한 대안

| 대안 | 장점 | 단점 | 기각 사유 |
|---|---|---|---|
|  |  |  |  |

## Consequences (결과)

- 좋아지는 것(측정값 인용: 유실/순서/중복/회복/지연 Before→After):
- 나빠지거나 새로 생기는 비용(지연·복잡도·운영 부담):
- 남는 리스크:

## 트레이드오프 / 한계

- exactly-once는 환상이므로 무엇을 택했는가(at-least-once + 멱등 / at-most-once):
- 동일 aggregate 순서 보장 범위 / 다른 aggregate 간 미보장 근거:
- 컨슈머 장애 재처리 전략(재시도/DLQ/수동 임계):

## 운영 적용 시

- 모니터링/알람 지표:
- 복구 절차(런북):
